{
  "name": "lumen",
  "private": true,
  "license": "BSD-2-Clause",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "lerna run compile",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "update-version": "lerna publish --skip-git --skip-npm",
    "check-packages": "yarn clean && yarn compile && yarn test && yarn lint"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
